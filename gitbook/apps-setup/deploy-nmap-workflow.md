# Deploy NMAP workflow

* The complete nmap workflow infrastructure manifest can be deployed into kubernetes cluster by running the following command

```bash
kubectl apply -f infra/nmap-workflow/nmap-workflow.yaml
```